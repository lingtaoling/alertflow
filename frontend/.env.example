# Only needed when serving frontend separately from backend
# Local dev: leave unset (Vite proxy handles /api)
# Production (oaimu.com): https://oaimu.com (no trailing slash)
# Leave unset when frontend and API are same origin (relative /api works)
VITE_API_BASE_URL=http://localhost:3000
