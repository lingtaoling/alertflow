# Production build - used when you run: npm run build
# Same domain (nginx proxies /api to backend): leave empty for relative /api
# Different domain: VITE_API_BASE_URL=https://api.oaimu.com
VITE_API_BASE_URL=
